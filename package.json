{
  "name": "@atomico/kit",
  "description": "a repo to start with atomico",
  "version": "1.0.0",
  "editable": "https://github.com/atomicojs/atomico-kit/tree/master",
  "scripts": {
    "start": "estack dev {src,template}/**/*.{html,md};src/*.yaml",
    "build": "estack dev {src,template}/**/*.{html,md} docs",
    "build:npm": "estack build src/{hooks}/**/use-*.{js,ts};!**/*.test.{js,ts} ./ --external atomico",
    "test": "web-test-runner src/**/*.test.js --node-resolve --puppeteer",
    "test:w": "web-test-runner src/**/*.test.{tsx,js,ts} --node-resolve  --watch",
    "prepare": "npm run build:npm"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {},
  "peerDependencies": {},
  "devDependencies": {
    "@esm-bundle/chai": "^4.1.5",
    "@uppercod/css-to-object": "^1.0.1",
    "@uppercod/exp-route": "^1.0.0",
    "@uppercod/hash": "^1.0.0",
    "@web/dev-server-esbuild": "^0.2.4",
    "@web/test-runner": "^0.7.42",
    "@web/test-runner-puppeteer": "^0.6.3",
    "atomico": "^1.6.4",
    "estack": "^0.19.2"
  },
  "exports": {
    "import": "./"
  }
}
